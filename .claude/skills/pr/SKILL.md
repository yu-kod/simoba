# Create PR Skill
1. Run `npm run lint` and fix any errors
2. Run `npm run test` and ensure all pass
3. Stage and commit changes with a conventional commit message
4. Push the branch
5. Run `gh pr create --fill` with non-interactive flags only
6. Never use interactive shell prompts
