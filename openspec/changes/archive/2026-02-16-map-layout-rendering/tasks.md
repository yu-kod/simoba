## 1. マップレイアウト定数

- [x] 1.1 `src/domain/mapLayout.ts` を作成し、全マップ要素の座標・サイズ定数を定義する（レーン、ベース blue/red、タワー blue/red、ブッシュ 2箇所、ボス地点）
- [x] 1.2 カラーパレット定数を `mapLayout.ts` に定義する（レーン色、ベース色、タワー色、ブッシュ色、ボス地点色）

## 2. マップ描画関数

- [x] 2.1 `src/scenes/mapRenderer.ts` を作成し、`renderMap(scene: Phaser.Scene): void` 関数をエクスポートする
- [x] 2.2 レーン背景の描画を実装する（ワールド全幅の帯状矩形）
- [x] 2.3 Blue/Red ベースの描画を実装する（左端/右端の矩形、チームカラー）
- [x] 2.4 Blue/Red タワーの描画を実装する（ベースとレーン中央の間、円形、チームカラー）
- [x] 2.5 ブッシュ 2 箇所の描画を実装する（レーン上下端、緑系）
- [x] 2.6 ボススポーン地点のマーカーを描画する（ワールド中央、黄/金系）

## 3. GameScene 統合

- [x] 3.1 `GameScene.create()` から `renderMap()` を呼び出し、既存の背景描画と統合する

## 4. テスト

- [x] 4.1 `mapLayout.ts` の定数が正しい範囲内にあることを検証するユニットテストを作成する（座標がワールド境界内、ベースが左右端等）
- [x] 4.2 マップ描画の E2E テストを作成する（Canvas にマップ要素が描画されていることをスクリーンショット比較で検証）
- [x] 4.3 全テスト PASS を確認する（`npm run test:unit` + `npm run test:e2e` + `npx tsc --noEmit`）
